# server side

## make
 ### run
   goを実行します
 ### build-m1
    Dockerfileをm1向けにビルドします
    ビルドしたにDocker内でmake runが呼ばれます
  ### build-intel
    Dockerfileをintel向けにビルドします
    ビルドしたにDocker内でmake runが呼ばれます
  ### fmt
    go fmt を適用します